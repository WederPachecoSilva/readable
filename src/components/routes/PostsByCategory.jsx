import * as React from 'react';

import withErrorBoundary from '../errorHandling/withErrorBoundary';

const PostsByCategory = () => <p>posts by category</p>;

export default withErrorBoundary(PostsByCategory);
